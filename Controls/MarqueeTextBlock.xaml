<UserControl x:Class="TrackDot.Controls.MarqueeTextBlock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="200"
             ClipToBounds="True"
             SizeChanged="OnSizeChanged"
             Loaded="OnLoaded"
             Unloaded="OnUnloaded">
    <Grid ClipToBounds="True">
        <Canvas x:Name="MarqueeCanvas" ClipToBounds="True" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <TextBlock x:Name="DisplayTextBlock"
                       Canvas.Left="0" Canvas.Top="0"
                       Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontWeight="{Binding FontWeight, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       TextTrimming="None"
                       SizeChanged="OnTextBlockSizeChanged">
                <TextBlock.RenderTransform>
                    <TranslateTransform x:Name="TextTranslate" X="0" Y="0" />
                </TextBlock.RenderTransform>
            </TextBlock>
        </Canvas>
    </Grid>
</UserControl>

